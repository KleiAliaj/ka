import React from "react";
import { motion } from "framer-motion";

function AllFramer() {
  return (
    <motion.div
    // initial={{ scale: 0 }}
    // animate={{ scale: 1 }}
    // transition={{ duration: 0.5 }}
    // className="bg-sky-400 rounded-full shadow-md w-40 h-40"
    ></motion.div>
  );
}

export default AllFramer;
